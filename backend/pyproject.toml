[project]
name = "hr-agent-backend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "python-dotenv>=1.0.0",
    "openai>=1.50.0",
    "websockets>=13.0",
    "cartesia>=1.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["app"]
